const topicked = conversation => 

const scripted

const generated

export default class Finisher {
  constructor(configuration = {}) {
    this.configuration = {};
  }

  finished(conversation) {
    const { topic, panel } = this.configuration;

    if (topic && panel) {
      // Check for panel existence
    }

    if (topic) {
      // Check for proposed script
    }

    // Check for proposed topic
  }
}